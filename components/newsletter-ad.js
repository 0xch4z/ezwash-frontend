import React from 'react';
import { Button } from 'react-bootstrap';

const newsletterAd = () => (
  <div id="newsletter-ad-wrapper">
    <h4>Want to keep up on our latest offers?</h4>
    <Button>Sign up</Button>
  </div>
);

export default newsletterAd;